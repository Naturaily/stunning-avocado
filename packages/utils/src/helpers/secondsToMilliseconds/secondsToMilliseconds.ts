const ONE_SEC_IN_MILLISECONDS = 1000;

/**
 * Convert seconds to milliseconds.
 * @param {number} seconds - number - The number of seconds to convert to milliseconds.
 */
export const secondsToMilliseconds = (seconds: number) => seconds * ONE_SEC_IN_MILLISECONDS;
